{
  "name": "irwanmotor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:pages": "node scripts/dev-pages.js",
    "hash-password": "node scripts/hash-password.js",
    "seed": "node --env-file=.dev.vars scripts/seed.js",
    "seed:workshop": "node --env-file=.dev.vars scripts/seed-workshop.js",
    "seed:super": "node scripts/seed-super.js --local",
    "start": "wrangler dev",
    "test": "vitest",
    "test:run": "vitest --run",
    "check:frontend": "node scripts/check-frontend.js",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.5",
    "@types/node": "^26.0.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
